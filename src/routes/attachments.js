// backend/src/routes/attachments.js
// Stub — file attachments not yet implemented
const router = require('express').Router();
router.get('/', (req, res) => res.json({ message: 'Attachments coming soon' }));
module.exports = router;
